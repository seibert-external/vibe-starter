name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: seibert-external/vibe-ci/.github/workflows/ci.yml@main
    permissions:
      contents: read
      checks: write
    secrets: inherit
